import { SequenceLootbox } from "../SequenceLootbox";

export default (
  <SequenceLootbox
    isPlay
    delay={200}
    className="w-[300px] mx-auto max-w-[var(--max-container-width)] aspect-square"
  />
);
